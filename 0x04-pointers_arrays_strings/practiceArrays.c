#include <stdio.h>
/**
 *main - using sizeof to dynamically determine the size of type char,
 *int and float
 *
 *Return: Always 0.
 */

int main(void)
{
	int n;
	printf("Size of type 'char' on my computer: %lu b", ptr );
	return (0);
}
